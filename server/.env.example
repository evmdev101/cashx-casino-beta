PORT=8787
CHAIN_ID=369
RPC_URL=https://rpc.pulsechain.com
MINES_CONTRACT_ADDRESS=0x684e6B760FC931BB858b3bf3dFC056e550b13D90
GAME_SIGNER_PRIVATE_KEY=PASTE_BACKEND_SIGNER_PRIVATE_KEY
AUTO_SETTLE=false
NODE_ENV=production
# Comma-separated list of allowed origins (required in production).
CORS_ORIGINS=https://yourdomain.example,https://www.yourdomain.example
# Optional: allow localhost/127.0.0.1 origins even in production (not recommended).
ALLOW_LOCALHOST_CORS=false
# Optional: data directory for runtime state (PvP rooms, etc).
DATA_DIR=./data
# Optional: basic IP rate limit per minute.
RATE_LIMIT_PER_MINUTE=120
